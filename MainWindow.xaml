<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="XrayUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="using:XrayUI.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="代理控制台">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar — registered via SetTitleBar() in code-behind -->
        <Grid x:Name="AppTitleBar"
              Grid.Row="0"
              Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="DockButton"
                    Grid.Column="0"
                    Click="DockButton_Click"
                    Background="Transparent"
                    BorderThickness="0"
                    CornerRadius="4"
                    Width="30" Height="28"
                    Padding="0"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center">
                <FontIcon Glyph="&#xE90C;"  FontSize="13"  />
            </Button>

            <TextBlock x:Uid="MainWindow_TitleText"
                       Grid.Column="1"
                       Text="代理控制台"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0" />
        </Grid>

        <!-- Mini mode compact panel -->
        <Grid x:Name="MiniModePanel"
              Grid.Row="1"
              Visibility="{x:Bind ViewModel.MiniModeVisibility, Mode=OneWay}"
              Margin="16,0,16,0">
            <!-- Mini content: tighten vertical rhythm to match the mockup -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Server name + routing mode -->
                <StackPanel Grid.Row="0"
                            Margin="2,18,32,0"
                            VerticalAlignment="Top"
                            Spacing="6">
                    <TextBlock Text="{x:Bind ViewModel.ActiveServerName, Mode=OneWay}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               TextWrapping="NoWrap"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{x:Bind ViewModel.MiniRoutingMode, Mode=OneWay}"
                               FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>

                <!-- Power button (left) + status indicator (right) -->
                <Grid Grid.Row="2"
                      Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ToggleButton Grid.Column="0"
                                  Command="{x:Bind ViewModel.MiniStartStopCommand}"
                                  IsChecked="{x:Bind ViewModel.MiniIsRunning, Mode=OneWay}"
                                  Width="40" Height="40"
                                  CornerRadius="20"
                                  Padding="0"
                                  BorderThickness="0"
                                  VerticalAlignment="Bottom">
                        <FontIcon Glyph="&#xE7E8;" FontSize="16" />
                    </ToggleButton>

                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                Spacing="5"
                                VerticalAlignment="Center">
                        <Ellipse Width="6"
                                 Height="6"
                                 Fill="{StaticResource StateSuccessBrush}"
                                 VerticalAlignment="Center"
                                 Visibility="{x:Bind ViewModel.MiniDotVisibility, Mode=OneWay}" />
                        <TextBlock Text="{x:Bind ViewModel.MiniStatusText, Mode=OneWay}"
                                   FontSize="13"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Grid>
            </Grid>

            <Border x:Name="MiniDragRegion"
                    Height="32"
                    Margin="0,0,56,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    Background="Transparent" />

            <Grid HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Margin="0,4,-10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="MiniExpandButton"
                        Grid.Column="0"
                        Width="28" Height="22"
                        Padding="0"
                        BorderThickness="0"
                        Background="Transparent">
                    <FontIcon Glyph="&#xE740;" FontSize="12" />
                </Button>

                <Button x:Name="MinicloseButton"
                        Grid.Column="1"
                        Width="28" Height="22"
                        Padding="0"
                        BorderThickness="0"
                        Background="Transparent">
                    <FontIcon Glyph="&#xE711;" FontSize="12" />
                </Button>
            </Grid>
        </Grid>

        <Grid x:Name="FullModePanel" Grid.Row="1" Margin="16,0,16,16"
              Visibility="{x:Bind ViewModel.FullModeVisibility, Mode=OneWay}">
        <Grid x:Name="WorkspacePanel"
              Grid.Row="0"
              Visibility="{x:Bind ViewModel.WorkspaceVisibility, Mode=OneWay}"
              Margin="0"
              ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.35*" MinWidth="440" MaxWidth="680" />
                <ColumnDefinition Width="1.4*" MinWidth="440" />
            </Grid.ColumnDefinitions>

            <views:ServerListControl Grid.Column="0"
                                      x:Name="ServerList"
                                      ViewModel="{x:Bind ViewModel.ServerList, Mode=OneTime}"
                                      SwitchToSelectedServerCommand="{x:Bind ViewModel.SwitchToSelectedServerCommand, Mode=OneTime}"
                                      ToggleServerConnectionCommand="{x:Bind ViewModel.ToggleServerConnectionCommand, Mode=OneTime}"
                                      SetPrimaryProxyCommand="{x:Bind ViewModel.SetPrimaryProxyCommand, Mode=OneTime}" />

            <Grid Grid.Column="1"
                  VerticalAlignment="Stretch">

                <!-- Main view: server detail + control panel -->
                <Grid Visibility="{x:Bind ViewModel.MainContentVisibility, Mode=OneWay}"
                      VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <views:ServerDetailControl Grid.Row="0"
                                               x:Name="ServerDetail"
                                               ViewModel="{x:Bind ViewModel.ServerDetail, Mode=OneTime}" />

                    <views:ControlPanelControl x:Name="ControlPanel"
                                               Grid.Row="1"
                                               ViewModel="{x:Bind ViewModel.ControlPanel, Mode=OneTime}" />
                </Grid>

                <!-- Personalize view -->
                <Grid x:Name="PersonalizeHost"
                      Visibility="{x:Bind ViewModel.PersonalizeVisibility, Mode=OneWay}" />

                <!-- AppSettings view -->
                <Grid x:Name="AppSettingsHost"
                      Visibility="{x:Bind ViewModel.AppSettingsVisibility, Mode=OneWay}" />

            </Grid>
        </Grid>
            <Grid x:Name="TrafficHost" Visibility="{x:Bind ViewModel.TrafficVisibility, Mode=OneWay}" />
        </Grid>
    </Grid>
</Window>

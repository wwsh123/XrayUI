<?xml version="1.0" encoding="utf-8"?>

<UserControl x:Class="XrayUI.Views.ServerListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:XrayUI.Models"
             xmlns:vm="using:XrayUI.ViewModels"
             xmlns:views="using:XrayUI.Views"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="420">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Vertical">
            <TextBlock x:Uid="ServerList_HeaderText"
                       Text="服务器列表"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Spacing="8"
                        Margin="0,8,0,0">
                <DropDownButton x:Name="BtnAdd"
                                Padding="12,6">
                    <DropDownButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock x:Uid="ServerList_AddBtnText" Text="添加" VerticalAlignment="Center" />
                        </StackPanel>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem x:Uid="ServerList_ImportClipboardItem"
                                            Text="从剪贴板导入"
                                            Command="{x:Bind ViewModel.ImportFromClipboardCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE77F;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem x:Uid="ServerList_ImportNodeItem"
                                            Text="导入节点"
                                            Command="{x:Bind ViewModel.ImportFromLinkCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE71B;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem x:Uid="ServerList_AddSubscriptionItem"
                                            Text="添加订阅"
                                            Command="{x:Bind ViewModel.OpenSubscriptionsCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xED11;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem x:Uid="ServerList_AddManualItem"
                                            Text="手动添加"
                                            Command="{x:Bind ViewModel.AddManualCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE710;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem x:Uid="ServerList_AddChainProxyItem"
                                            Text="链式代理"
                                            Command="{x:Bind ViewModel.AddChainProxyCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xEF90;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
                <Button x:Name="BtnEdit"
                        Command="{x:Bind ViewModel.EditServerCommand}"
                        IsEnabled="{x:Bind ViewModel.CanEditSelectedServer, Mode=OneWay}"
                        Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE70F;"
                                  Margin="0,0,5,0" />
                        <TextBlock x:Uid="ServerList_EditText" Text="编辑" />
                    </StackPanel>
                </Button>
                <Button x:Name="BtnRemove"
                        Command="{x:Bind ViewModel.RemoveServerCommand}"
                        IsEnabled="{x:Bind ViewModel.CanRemoveSelectedServer, Mode=OneWay}"
                        Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE74D;"
                                  Margin="0,0,5,0" />
                        <TextBlock x:Uid="ServerList_DeleteText" Text="删除" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1"
              Margin="0,16,0,0"
              ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <AutoSuggestBox x:Name="ServerSearchBox"
                            x:Uid="ServerList_SearchBox"
                            Grid.Column="0"
                            PlaceholderText="搜索服务器"
                            VerticalAlignment="Center"
                            UpdateTextOnSelect="False"
                            QueryIcon="Find"
                            TextChanged="ServerSearchBox_TextChanged"
                            SuggestionChosen="ServerSearchBox_SuggestionChosen"
                            QuerySubmitted="ServerSearchBox_QuerySubmitted">
                <AutoSuggestBox.ItemTemplate>
                    <DataTemplate x:DataType="models:ServerEntry">
                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                    </DataTemplate>
                </AutoSuggestBox.ItemTemplate>
            </AutoSuggestBox>

            <ToggleButton x:Name="FilterToggle"
                          Grid.Column="1"
                          Background="Transparent"
                          BorderBrush="Transparent"
                          Padding="6"
                          IsChecked="{x:Bind ViewModel.IsFilterPanelOpen, Mode=TwoWay}"
                          Visibility="{x:Bind ViewModel.IsChipBarVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                          VerticalAlignment="Center">
                <FontIcon Glyph="&#xE8A9;"
                          FontSize="16" />
            </ToggleButton>

            <Button x:Name="TestLatencyButton"
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Command="{x:Bind ViewModel.TestAllLatenciesCommand}"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Padding="6">
                <Grid>
                    <Grid Visibility="{x:Bind ViewModel.IsTestingLatencies, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <FontIcon Glyph="&#xEC4A;"
                                  FontSize="16"
                                  Visibility="{x:Bind views:ServerListControl.ConnectModeIconVisibility(ViewModel.LatencyTestMode), Mode=OneWay}" />
                        <Viewbox Width="16"
                                 Height="16"
                                 Visibility="{x:Bind views:ServerListControl.RealModeIconVisibility(ViewModel.LatencyTestMode), Mode=OneWay}">
                            <PathIcon Data="M6.54322 5.48256C7.84003 4.39564 9.46603 3.6886 11.25 3.53263V5.25C11.25 5.66421 11.5858 6 12 6C12.4142 6 12.75 5.66421 12.75 5.25V3.53263C16.929 3.89798 20.2412 7.28742 20.4855 11.5H18.75C18.3358 11.5 18 11.8358 18 12.25C18 12.6642 18.3358 13 18.75 13H20.4444C20.1837 15.3116 19.0211 17.2483 17.2765 18.6683C16.9553 18.9298 16.9068 19.4022 17.1683 19.7235C17.4298 20.0447 17.9022 20.0932 18.2235 19.8317C20.45 18.0194 21.8936 15.4382 21.9944 12.3473C21.9985 12.3154 22.0006 12.283 22.0006 12.25C22.0006 12.2301 21.9998 12.2103 21.9983 12.1907C21.9994 12.1274 22 12.0638 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 15.2522 3.52303 17.9538 5.76986 19.8262C6.08807 20.0913 6.56099 20.0483 6.82617 19.7301C7.09134 19.4119 7.04835 18.939 6.73014 18.6738C5.01891 17.2478 3.82673 15.3069 3.55764 13H5.24963C5.66385 13 5.99963 12.6642 5.99963 12.25C5.99963 11.8358 5.66385 11.5 5.24963 11.5H3.51446C3.62363 9.61804 4.34508 7.90037 5.48256 6.54322L6.71967 7.78033C7.01256 8.07322 7.48744 8.07322 7.78033 7.78033C8.07322 7.48744 8.07322 7.01256 7.78033 6.71967L6.54322 5.48256ZM16.7589 6.63409C16.5207 6.44971 16.1824 6.45611 15.9516 6.64937L15.7344 6.83167C15.596 6.9479 15.3979 7.1145 15.1588 7.31609C14.6808 7.71919 14.0386 8.26249 13.3826 8.82286C12.727 9.38283 12.0555 9.96154 11.5197 10.4349C11.2521 10.6713 11.0157 10.8838 10.831 11.0556C10.6593 11.2154 10.4986 11.3707 10.4112 11.4787C9.75823 12.2863 9.89798 13.4588 10.7234 14.0977C11.5488 14.7365 12.7472 14.5998 13.4002 13.7923C13.4876 13.6842 13.6051 13.4955 13.7245 13.2953C13.8529 13.0799 14.0099 12.8059 14.1835 12.4967C14.5311 11.8777 14.9523 11.1053 15.3585 10.3522C15.7649 9.59869 16.1576 8.86226 16.4486 8.31438C16.5941 8.04039 16.7142 7.81345 16.798 7.65496L16.9294 7.40617C17.0685 7.14203 16.9971 6.81847 16.7589 6.63409Z" />
                        </Viewbox>
                    </Grid>
                    <ProgressRing Width="16"
                                  Height="16"
                                  IsActive="{x:Bind ViewModel.IsTestingLatencies, Mode=OneWay}"
                                  Visibility="{x:Bind ViewModel.IsTestingLatencies, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>
                <Button.ContextFlyout>
                    <MenuFlyout>
                        <RadioMenuFlyoutItem x:Name="TestModeConnectItem"
                                             x:Uid="ServerList_TestModeConnect"
                                             Text="Tcping延迟"
                                             GroupName="LatencyTestMode"
                                             Click="TestModeConnectItem_Click" />
                        <RadioMenuFlyoutItem x:Name="TestModeRealItem"
                                             x:Uid="ServerList_TestModeReal"
                                             Text="真连接延迟"
                                             GroupName="LatencyTestMode"
                                             IsChecked="True"
                                             Click="TestModeRealItem_Click" />
                    </MenuFlyout>
                </Button.ContextFlyout>
            </Button>
        </Grid>
        <Grid Grid.Row="2"
              Margin="0,12,0,0"
              ColumnSpacing="8"
              Visibility="{x:Bind ViewModel.IsFilterBarVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        Spacing="6"
                        VerticalAlignment="Center">
                <FontIcon Glyph="&#xEA37;"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Uid="ServerList_FilterLabelText"
                           Text="筛选条件:"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>

            <ComboBox Grid.Column="1"
                      MaxWidth="180"
                      ItemsSource="{x:Bind ViewModel.GroupChips, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedChip, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:ServerGroupChip">
                        <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"
                                   TextTrimming="CharacterEllipsis" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <DropDownButton x:Name="SortButton"
                            Grid.Column="2"
                            Background="Transparent"
                            BorderBrush="Transparent">
                <DropDownButton.Content>
                    <FontIcon Glyph="&#xE8CB;" FontSize="16" />
                </DropDownButton.Content>
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <RadioMenuFlyoutItem x:Uid="ServerList_SortDefaultItem"
                                             Text="默认"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortDefault, Mode=TwoWay}" />
                        <RadioMenuFlyoutItem x:Name="SortActiveItem"
                                             x:Uid="ServerList_SortActiveItem"
                                             Text="当前连接"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortActive, Mode=TwoWay}"
                                             IsEnabled="{x:Bind ViewModel.CanSortByActive, Mode=OneWay}" />
                        <RadioMenuFlyoutItem x:Uid="ServerList_SortProtocolItem"
                                             Text="协议"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortProtocol, Mode=TwoWay}" />
                        <RadioMenuFlyoutItem x:Name="SortLatencyItem"
                                             x:Uid="ServerList_SortLatencyItem"
                                             Text="延迟"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortLatency, Mode=TwoWay}"
                                             IsEnabled="{x:Bind ViewModel.CanSortByLatency, Mode=OneWay}" />
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </Grid>

        <ListView Grid.Row="3"
                  x:Name="ServersListView"
                  Margin="0,8,0,0"
                  ItemsSource="{x:Bind ViewModel.VisibleServers, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedServer, Mode=TwoWay}"
                  CanDragItems="{x:Bind ViewModel.CanReorderInCurrentChip, Mode=OneWay}"
                  CanReorderItems="{x:Bind ViewModel.CanReorderInCurrentChip, Mode=OneWay}"
                  AllowDrop="{x:Bind ViewModel.CanReorderInCurrentChip, Mode=OneWay}"
                  Loaded="ServersListView_Loaded"
                  DragItemsCompleted="ServersListView_DragItemsCompleted"
                  SelectionChanged="ServersListView_SelectionChanged"
                  SelectionMode="Extended"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ServerEntry">
                    <Grid Background="Transparent"
                          ContextRequested="ServerItem_ContextRequested"
                          DoubleTapped="ServerItem_DoubleTapped">
                        <StackPanel Orientation="Vertical"
                                    Spacing="6"
                                    Margin="0,12,0,12">
                            <Grid VerticalAlignment="Center"
                                  ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Left information area: name plus its nearby latency, then endpoint details. -->
                                <StackPanel Grid.Column="0"
                                            Spacing="6">
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="6">
                                        <TextBlock Text="{x:Bind Name, Mode=OneWay}"
                                                   MaxWidth="210"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{x:Bind LatencyText, Mode=OneWay}"
                                                   Visibility="{x:Bind HasLatency, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"
                                                   Foreground="{x:Bind views:ServerListControl.LatencyForeground(LatencyMs), Mode=OneWay}" />
                                        <TextBlock Text="{x:Bind views:ServerListControl.GeminiStatusText(GeminiAvailable), Mode=OneWay}"
                                                   Visibility="{x:Bind views:ServerListControl.GeminiStatusVisibility(GeminiAvailable), Mode=OneWay}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"
                                                   Foreground="{x:Bind views:ServerListControl.GeminiStatusBrush(GeminiAvailable), Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal"
                                                Spacing="8"
                                                Visibility="{x:Bind IsChain, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <TextBlock Text="{x:Bind Host, Mode=OneWay}"
                                                   MaxWidth="180"
                                                   TextTrimming="CharacterEllipsis"
                                                   TextWrapping="NoWrap"
                                                   MaxLines="1"
                                                   FontSize="16"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{x:Bind Port, Mode=OneWay}"
                                                   FontSize="16"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{x:Bind DisplayProtocol, Mode=OneWay}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="{x:Bind Protocol, Mode=OneWay, Converter={StaticResource ProtocolToBrushConverter}}" />
                                    </StackPanel>
                                    <TextBlock x:Uid="ServerList_ChainBadgeText"
                                               Text="ProxyChain"
                                               Visibility="{x:Bind IsChain, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="{x:Bind Protocol, Mode=OneWay, Converter={StaticResource ProtocolToBrushConverter}}" />
                                </StackPanel>

                                <!-- Each row exposes exactly one action: auxiliary when configured in multi-proxy mode, otherwise primary. -->
                                <StackPanel Grid.Column="1"
                                            Width="166"
                                            Spacing="6"
                                            HorizontalAlignment="Right">
                                    <Button Tag="{x:Bind}"
                                            Click="ProxyActionButton_Click"
                                            Padding="8,2"
                                            MinHeight="0"
                                            FontSize="16"
                                            Foreground="White"
                                            Background="{x:Bind IsCurrentProxyActionActive, Mode=OneWay, Converter={StaticResource RunningStatusBrushConverter}}"
                                            BorderBrush="{x:Bind IsCurrentProxyActionActive, Mode=OneWay, Converter={StaticResource RunningStatusBrushConverter}}"
                                            HorizontalAlignment="Right"
                                            Content="{x:Bind views:ServerListControl.ProxyActionText(UsesAuxiliaryProxyAction, IsCurrentProxyActionActive), Mode=OneWay}" />
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="12"
                                                HorizontalAlignment="Right"
                                                Visibility="{x:Bind UsesAuxiliaryProxyAction, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <TextBlock Text="{x:Bind views:ServerListControl.PrimaryPortText(RuntimeProxyPort), Mode=OneWay}"
                                                   FontSize="13"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{x:Bind views:ServerListControl.ConnectionStatusText(IsActive), Mode=OneWay}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{x:Bind IsActive, Mode=OneWay, Converter={StaticResource RunningStatusBrushConverter}}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="12"
                                                HorizontalAlignment="Right"
                                                Visibility="{x:Bind UsesAuxiliaryProxyAction, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="{x:Bind views:ServerListControl.AuxiliaryPortText(DedicatedPort), Mode=OneWay}"
                                                   FontSize="13"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{x:Bind views:ServerListControl.ConnectionStatusText(IsDedicatedPortActive), Mode=OneWay}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{x:Bind IsDedicatedPortActive, Mode=OneWay, Converter={StaticResource RunningStatusBrushConverter}}" />
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
